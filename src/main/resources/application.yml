spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:cmbms}
    username: ${DB_USERNAME:postgres}
    password: "${DB_PASSWORD:Yasin@123}"
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        use_sql_comments: false
  flyway:
    enabled: false
  sql:
    init:
      mode: never
  jackson:
    serialization:
      WRITE_DATES_AS_TIMESTAMPS: false

  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB

  # Spring Security OAuth2 Resource Server
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8090/realms/insa
          jwk-set-uri: http://localhost:8090/realms/insa/protocol/openid-connect/certs

app:
  file:
    upload-dir: ${UPLOAD_DIR:uploads}
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000}
  notifications:
    retention-days: 10

server:
  port: ${SERVER_PORT:8081}

# Logging Configuration
logging:
  level:
    org.springframework.security: INFO
    org.springframework.security.oauth2: INFO
    org.springframework.security.web: INFO
    com.org.cmbms: INFO
    org.hibernate.SQL: INFO
    org.hibernate.type.descriptor.sql.BasicBinder: INFO

# Keycloak Configuration
keycloak:
  enabled: true
  realm: insa
  auth-server-url: http://localhost:8090
  resource: insa-backend
  credentials:
    secret: ${KEYCLOAK_CLIENT_SECRET:}
  use-resource-role-mappings: false
  bearer-only: true
  ssl-required: none

  # Admin Client Configuration (for user management)
  admin:
    server-url: http://localhost:8090
    realm: insa
    client-id: insa-backend
    client-secret: ${KEYCLOAK_ADMIN_CLIENT_SECRET:}
    username: ${KEYCLOAK_ADMIN_USERNAME:admin}
    password: ${KEYCLOAK_ADMIN_PASSWORD:admin}
